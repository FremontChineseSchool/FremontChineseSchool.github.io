---
import { alternatePath, type Lang } from "../i18n/ui";

interface Props {
  lang: Lang;
}
const { lang } = Astro.props;
const target = alternatePath(lang, Astro.url.pathname);
const otherLabel = lang === "en" ? "中文" : "English";
---

<a
  href={target}
  class="rounded-full border border-white/70 px-3 py-1 text-sm font-medium text-white transition hover:bg-white hover:text-fcs-blue-dark"
  hreflang={lang === "en" ? "zh-Hant" : "en"}
  lang={lang === "en" ? "zh-TW" : "en"}
>
  {otherLabel}
</a>
